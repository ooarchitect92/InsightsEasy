# Do not copy static credentials. Run `npm run setup` in a new checkout.
# Generated .env and .local contain private sandbox credentials and must not be committed.
# Real providers require explicit owner-scoped worker bindings; see docs/PROVIDERS.md.
